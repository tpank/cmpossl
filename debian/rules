#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	cp Makefile_cmp Makefile

override_dh_auto_build:
	OPENSSL_DIR=/usr dh_auto_build

override_dh_auto_install:
	OPENSSL_DIR=/usr dh_auto_install
